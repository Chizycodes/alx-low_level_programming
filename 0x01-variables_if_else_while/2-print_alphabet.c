#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'if else'
 *
 * Return: Always 0 (Success)
*/
int main(void)
{	
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putChar(x);
	}

	return (0);
}
